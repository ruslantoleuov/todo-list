"use strict";

const TodoList = (function () {
  "use strict";

  const defaultProject = [];
  const projects = [];

  return {
    defaultProject,
    projects,
  };
})();

export { TodoList as default };
